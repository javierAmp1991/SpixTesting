import style from "/styles/Mobile/EventPage/commentSection.module.css"

export default function CommentSectionMobile({children}) {
    return (
        <div>
            {children}
        </div>
    )
}